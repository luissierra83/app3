workflows:
  build-apk:
    name: Build APK - Pedido PDF
    max_build_duration: 20
    environment:
      flutter: stable
    scripts:
      - name: Get dependencies
        script: |
          flutter pub get

      - name: Clean project
        script: |
          flutter clean
          flutter pub get

      - name: Fix Gradle wrapper
        script: |
          if [ ! -f android/gradlew ]; then
            echo "Gradle wrapper not found. Re-creating android directory."
            rm -rf android
            flutter create .
            flutter pub get
          fi

      - name: Fix Dart type error
        script: |
          sed -i '' "s/int get bruto.*/int get bruto => productos.fold<int>(0, (acc, p) => acc + (p['u'] * p['p']).toInt());/" lib/main.dart

      - name: Build release APK
        script: |
          flutter build apk --release --no-shrink

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk

    publishing:
      email:
        recipients:
          - luisgsierrag@gmail.com
